{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <h1>{{ "user.password.recover.title"|trans({}, "forms") }}</h1>

            {{ form_start(form) }}
            {{ form_row(form.email) }}

            <button type="submit" class="btn btn-primary" formnovalidate>{{ "user.password.recover.btn.submit"|trans({}, "forms") }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
