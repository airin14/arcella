{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h3 class="panel-title">Settings</h3>
                </div>
                <div class="panel-body">
                    <ul class="nav nav-pills nav-stacked">
                        <li role="presentation" class="active"><a href="{{ path('security_update_password') }}">{{ 'link.user.settings.password'|trans({}) }}</a></li>
                        <li role="presentation"><a href="{{ path('user_update_email') }}">{{ 'link.user.settings.email'|trans({}) }}</a></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="col-md-8">
            <h1>{{ "user.update.password.title"|trans({}, "forms") }}</h1>

            {{ form_start(form) }}
            {{ form_row(form.oldPassword) }}
            {{ form_row(form.newPassword.first) }}
            {{ form_row(form.newPassword.second) }}

            <button type="submit" class="btn btn-primary" formnovalidate>{{ "user.update.password.btn.submit"|trans({}, "forms") }}</button>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
